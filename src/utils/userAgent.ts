// import { UAParser } from 'ua-parser-js'

// const parser = (typeof window !== undefined && window.navigator) ? new UAParser(window.navigator.userAgent) : ''
// const { type } = parser.getDevice()

// export const userAgent = parser.getResult()

export const isMobile = false; // type === 'mobile' || type === 'tablet'
